apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.default: role:readonly
  policy.csv: |
    # Relynce team members get admin access
    g, joseph@relynce.ai, role:admin
  scopes: '[email]'
