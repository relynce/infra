name: Deploy Infrastructure to Dev

on:
  push:
    branches:
      - main
    paths:
      - 'base/**'
      - 'overlays/dev/**'
      - '.github/workflows/deploy-dev.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployed by Flux CD
        run: |
          echo "Infrastructure is deployed by Flux CD."
          echo "See polaris/k8s/flux/clusters/gke-dev/infra-sync.yaml for configuration."
          echo "To force reconciliation: flux reconcile kustomization infra-dev --with-source"
