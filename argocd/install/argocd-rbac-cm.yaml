apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
data:
  policy.default: role:readonly
  policy.csv: |
    # Team members get admin access (both emails until Google Workspace migrated)
    g, joseph@revelara.ai, role:admin
    g, joseph@relynce.ai, role:admin
  scopes: '[email]'
